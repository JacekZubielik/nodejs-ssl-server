name: CICD

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: [ubuntu-latest]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Login to docker hub
        run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }} 
      - name: Build docker image
        run: docker build -t gorabbit/nodejs-ssl-server .
      - name: Publish image to docker hub
        run: docker push gorabbit/nodejs-ssl-server:latest
        
  # deploy:
  #   needs: build
  #   runs-on: [aws-ec2]
  #   steps:
  #     - name: Pull image from docker hub
  #       run: docker pull gorabbit/nodejs-ssl-server:latest
  #     - name: Delete old container
  #       run: docker rm -f nodejs-ssl-server
  #     - name: Run docker container
  #       run: docker run -d -p 5000:5000 --name nodejs-app-container gorabbit/nodejs-ssl-server